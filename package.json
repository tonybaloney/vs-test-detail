{
  "name": "vs-nunit",
  "version": "1.0.0",
  "description": "",
  "main": "index.tsx",
  "scripts": {
    "test": "jest --ci --testResultsProcessor=./node_modules/jest-nunit-reporter",
    "compile": "npm run clean && webpack --mode production",
    "compile:dev": "npm run clean && webpack --mode development",
    "postbuild": "npm run package",
    "package": "tfx extension create --output-path build",
    "gallery-publish-testing": "tfx extension publish --publisher AnthonyShaw-testing --overrides-file vss-extension-testing.json --output-path build --rev-version",
    "gallery-publish": "tfx extension publish --output-path build --rev-version",
    "clean": "rimraf ./dist && rimraf ./build/*.vsix",
    "go-dev": "npm run compile:dev && npm run package && npm run gallery-publish-testing"
  },
  "keywords": [],
  "author": "Anthony Shaw",
  "license": "MIT",
  "dependencies": {
    "azure-devops-extension-api": "^1.154.1",
    "azure-devops-extension-sdk": "^2.0.11",
    "azure-devops-ui": "^1.155.0",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "vss-web-extension-sdk": "^5.141.0"
  },
  "devDependencies": {
    "@types/jest": "^24.0.15",
    "jest": "^24.8.0",
    "jest-nunit-reporter": "^1.3.1",
    "tfx-cli": "^0.7.8",
    "ts-jest": "^24.0.2",
    "webpack": "^4.37.0",
    "webpack-cli": "^3.3.6",
    "@types/react": "~16.8.2",
    "@types/react-dom": "~16.8.0",
    "base64-inline-loader": "^1.1.1",
    "copy-webpack-plugin": "^4.5.4",
    "css-loader": "~1.0.0",
    "file-loader": "~2.0.0",
    "node-sass": "~4.12.0",
    "rimraf": "~2.6.2",
    "sass-loader": "~7.1.0",
    "style-loader": "~0.23.1",
    "ts-loader": "~5.2.2",
    "typescript": "^2.9.2"
  }
}
